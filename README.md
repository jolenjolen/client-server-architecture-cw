# client-server-architecture-cw
coursework for client server architectures module where a RESTful API is used to perform CRUD operations for smart campus whose rooms are integrated by various sensors.
